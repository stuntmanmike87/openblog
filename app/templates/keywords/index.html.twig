{% extends 'base.html.twig' %}

{% block title %}Keywords index{% endblock %}

{% block body %}
    <h1>Keywords index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for keyword in keywords %}
            <tr>
                <td>{{ keyword.id }}</td>
                <td>{{ keyword.name }}</td>
                <td>{{ keyword.slug }}</td>
                <td>
                    <a href="{{ path('app_keywords_show', {'id': keyword.id}) }}">show</a>
                    <a href="{{ path('app_keywords_edit', {'id': keyword.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_keywords_new') }}">Create new</a>
{% endblock %}
